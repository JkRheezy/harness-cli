import { Gap, TargetArchitecture, BusinessTask } from './types';

/**
 * Generates business tasks from detected gaps
 * Stub implementation - to be completed in Task 5
 */
export class TaskGenerator {
  generate(
    gaps: Gap[],
    target: TargetArchitecture
  ): BusinessTask[] {
    // Stub implementation - returns empty tasks
    return [];
  }
}

# {{projectName}}

{{projectDescription}}

## 安装

```bash
npm install
```

## 开发

```bash
# 启动开发模式
npm run dev

# 构建
npm run build

# 运行测试
npm test

# 代码检查
npm run lint
```

## 使用 Harness 无人值守开发

本项目已配置 Harness CLI，支持 AI Agent 自动开发。

### 配置

1. 复制环境变量文件：
```bash
cp .env.example .env
# 编辑 .env，添加你的 API 密钥
```

2. 编辑 `.harness/config.yaml`，配置 LLM 参数

### 启动无人值守 Loop

```bash
# 启动开发 Loop（默认6小时）
harness loop

# 指定运行时长
harness loop --duration 4

# 模拟运行（不实际执行）
harness loop --dry-run
```

### 提交任务

```bash
harness task \
  --title "添加用户认证功能" \
  --description "实现 JWT 认证，包括登录、注册、token 刷新" \
  --requirements "使用 JWT,支持刷新token,密码加密存储" \
  --priority high
```

## 项目结构

```
.
├── src/              # 源代码
├── docs/             # 文档
├── .harness/         # Harness 配置
├── dist/             # 编译输出
└── tests/            # 测试
```

## License

MIT
